export enum POPOVER_TYPE_ENUM {
  tooltip = "tooltip",
  modal = "modal",
  drivenAction = "driven action",
}

export enum ACTION_TYPE_ENUM {
  click = "click",
  input = "input",
}

export enum METHOD_GET_ELEMENT_ENUM {
  id = "id",
  domHierarchy = "dom-hierarchy",
}
