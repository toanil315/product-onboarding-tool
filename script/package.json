{
  "name": "rsbuild-vanilla-ts",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "rsbuild dev --open",
    "build": "rsbuild build && cp -r ./src/index.css dist",
    "preview": "rsbuild preview"
  },
  "devDependencies": {
    "@rsbuild/core": "1.0.1-beta.14",
    "terser-webpack-plugin": "^5.3.10",
    "typescript": "^5.5.2"
  },
  "dependencies": {
    "driver.js": "^1.3.1",
    "zod": "^3.23.8"
  }
}
